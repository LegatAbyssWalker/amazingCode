package com.alluysl.amazin_Gjava;

import java.util.function.Function;

public class AmazinglyPseudorandomQuantumExtraInterfacedInterlacedIntrospectiveHashArrayBufferBuilderImplementation {

	final static protected Yes no = YesBuilder.build();
	static public Function<Void, Void> printer;
	
	static {
		printer = no.printer;
	}
	
	public AmazinglyPseudorandomQuantumExtraInterfacedInterlacedIntrospectiveHashArrayBufferBuilderImplementation() {
		
		// Calculate value of pi (https://cs.uwaterloo.ca/~alopez-o/math-faq/mathtext/node12.html)
		double pi = (double)(float)(16*Math.atan(1/5) - 4*Math.atan(1/239));
		// Compare with math library value ! !! !!! !!
		int ppi = (int)pi;
		long mpi = (long)Math.PI;
		
		if (ppi == mpi)
			printer.apply((Void)(Void)null);
		else
			printer.apply((Void)(Object)null);
	}
}
